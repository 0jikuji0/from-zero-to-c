/*
 * Add (Exercise 1)
 * Difficulty: * (1 star) - Very easy
 *
 * Task: implement the function `add` which returns the sum of two integers.
 * The skeleton below reads two integers from stdin and prints the result.
 *
 * TODOs are clearly marked where the student must complete the code.
 */

#include <stdio.h>

/* TODOs: students should implement the add function and the input/output code.
 * Hints / steps:
 *  1) Implement `int add(int a, int b)` to return the sum.
 *  2) In `main`, read two integers from stdin (use scanf) and handle invalid input.
 *  3) Call `add` and print the result followed by a newline.
 */

/* TODO 1: implement this function. Return a + b. */
int add(int a, int b) {
    // TODO: return the sum of a and b
    return /* TODO: replace with expression */ 0; /* placeholder */
}

int main(void) {
    /* TODO 2: read two integers into x and y. If reading fails, print an error
     * message to stderr and return a non-zero exit code. */
    int x, y;
    /* TODO: use scanf("%d %d", &x, &y) and check its return value */

    /* TODO 3: call add(x, y) and print the integer result followed by a newline */
    /* Example output: 7\n */
    return 0;
}
