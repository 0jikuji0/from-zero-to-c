/*
 * Selection Sort (C++ - Exercise 3)
 * Difficulty: *** (3 stars) - Easy
 */

#include <iostream>
#include <vector>

// TODO: implement selection_sort following the steps described below
void selection_sort(std::vector<int> &a) {
    // Steps:
    // 1) for i in [0..n-2] find min_idx in [i..n-1]
    // 2) swap a[i] and a[min_idx]
}

int main() {
    // TODO: read n, validate (n>0 and <=1000), read n integers into vector a,
    // call selection_sort(a) and print sorted array space-separated followed by newline.
    return 0;
}
/*
 * Selection Sort (C++ - Exercise 3)
 * Difficulty: *** (3 stars) - Easy
 */

#include <iostream>
#include <vector>

// TODO: implement selection_sort following the steps described below
void selection_sort(std::vector<int> &a) {
    // Steps:
    // 1) for i in [0..n-2] find min_idx in [i..n-1]
    // 2) swap a[i] and a[min_idx]
}

int main() {
    // TODO: read n, validate (n>0 and <=1000), read n integers into vector a,
    // call selection_sort(a) and print sorted array space-separated followed by newline.
    return 0;
}
