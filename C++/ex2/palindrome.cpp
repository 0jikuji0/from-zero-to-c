/*
 * Palindrome (C++ - Exercise 2)
 * Difficulty: ** (2 stars) - Quite easy
 *
 * Task: implement `is_palindrome` which checks whether a given string is a palindrome.
 */

#include <iostream>
#include <string>

// TODO 1: implement this function using two indices (i from start, j from end)
bool is_palindrome(const std::string &s) {
    // TODO: return true if s is palindrome, false otherwise
    return false; // placeholder
}

int main() {
    std::string s;
    // TODO 2: read a single word (no spaces) into s using std::cin

    // TODO 3: print 1 if palindrome, otherwise 0, followed by newline
    return 0;
}
